// Placeholder Convex client wiring. Replace with real Convex-generated client.
import { MeetingContextPayload } from "@shared/context";

export async function fetchMeetingContext(meetingId: string): Promise<MeetingContextPayload> {
  // TODO: Replace with Convex query when available
  throw new Error("Convex client not wired yet");
}
